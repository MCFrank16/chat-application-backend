// const { DB } = require('../config/db');
// const { createConversation, getConversation } = require('../queries');

// const createConversation = async (convoID) => {

//     try {
//         const conversation = await DB.query(getConversation(convoID));

//         if (conversation) return conversation;
//         await DB.query(createConversation(convoID, participants, lastMessage));
//         return True;
        
//     } catch (error) {
        
//     }
// }

// export default createConversation;
